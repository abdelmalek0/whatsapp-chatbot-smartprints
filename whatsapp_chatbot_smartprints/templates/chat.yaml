prompt: |
  ONLY Use the provided context and chat history to answer the client message.

  Chat History:
  {history}

  Context: 
  {context}

  Message: {message}

  DO NOT HALLUCINATE

variables:
  history: "Enter the chat history."
  context: "Enter the context."
  message: "Enter the message."